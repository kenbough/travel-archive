# Map data placeholder

The current JAPAN prototype fetches prefecture GeoJSON at runtime. Before production, place a pinned, appropriately licensed prefecture GeoJSON here and update `src/components/JapanMap.tsx` to load it using `import.meta.env.BASE_URL`.
